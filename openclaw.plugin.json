{
  "id": "cloudflare-ai",
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "apiToken": {
        "type": "string",
        "description": "Cloudflare API token with AI read permissions"
      },
      "accountId": {
        "type": "string",
        "description": "Cloudflare account ID"
      },
      "audioModel": {
        "type": "string",
        "default": "@cf/openai/whisper-large-v3-turbo",
        "description": "Whisper model for audio transcription (STT)"
      },
      "imageModel": {
        "type": "string",
        "default": "@cf/meta/llama-3.2-11b-vision-instruct-fp8",
        "description": "Vision model for image and video description"
      },
      "ttsModel": {
        "type": "string",
        "default": "@cf/deepgram/aura-2-en",
        "description": "TTS model for text-to-speech"
      },
      "defaultLanguage": {
        "type": "string",
        "default": "",
        "description": "Language code for transcription (empty for auto-detect)"
      },
      "timeout": {
        "type": "number",
        "default": 60000,
        "description": "Request timeout in milliseconds"
      }
    },
    "required": ["apiToken", "accountId"]
  },
  "uiHints": {
    "apiToken": {
      "label": "API Token",
      "sensitive": true,
      "help": "Cloudflare API token with AI read permissions"
    },
    "accountId": {
      "label": "Account ID",
      "help": "Your Cloudflare account ID (found in URL or dashboard)"
    },
    "audioModel": {
      "label": "Audio Model (STT)",
      "help": "Whisper model for speech-to-text"
    },
    "imageModel": {
      "label": "Image/Video Model",
      "help": "Vision model for image and video understanding"
    },
    "ttsModel": {
      "label": "TTS Model",
      "help": "Text-to-speech model"
    },
    "defaultLanguage": {
      "label": "Default Language",
      "placeholder": "en",
      "help": "Language code (e.g., en, es) or leave empty for auto-detect"
    },
    "timeout": {
      "label": "Timeout (ms)",
      "advanced": true
    }
  }
}
